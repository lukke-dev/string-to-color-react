{
  "name": "string-to-color-react",
  "version": "1.0.2",
  "description": "Generates a hexadecimal color depending on the input string, also allows passing default colors/schema",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lukke-dev/string-to-color-react.git"
  },
  "keywords": [
    "react",
    "color",
    "string",
    "generate"
  ],
  "author": "Lucas dos Santos",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.1.3"
  }
}
